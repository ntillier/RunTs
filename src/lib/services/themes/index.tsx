import { createTheme } from '@uiw/codemirror-themes';
import { draculaConfig } from './dracula';
import { abcdefConfig } from './abcdef';
import { androidStudioConfig } from './androidstudio';

/*
export const dracula = createTheme(draculaConfig);
export const abcdef = createTheme(abcdefConfig);
export const androidStudio = createTheme(androidStudioConfig);*/